services:
  rtmp:
    build: .
    container_name: dji-rtmp
    ports:
      - "1935:1935"
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./hls:/opt/data/hls
    restart: unless-stopped
